<div class="container">
  <div class="row">
    <div class="col-lg-12">

      <div class="brand">
        <div class="container">
          <h1 class="address-bar">Start Your Tasting Journey</h1>
            <div class="lead text-xs-center">Welcome, get ready for your next coffee tasting journey...</div>
        </div>
      </div>

      <% if current_user %>
      <%= render "login"%>
        Signed in as <%= current_user.name %> | <%= link_to "Logout", '/logout', class:"nav-link" %>
      <% else %>
        <%= link_to 'Login', '/login' %> | <%= link_to 'Signup', '/signup', class:"nav-link" %>
      <% end %>

</div>
</div>
</div>
